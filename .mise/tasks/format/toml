#!/usr/bin/env nu
#MISE description="Format TOML files"

let root = $env.MISE_PROJECT_ROOT
let files = (
  ^jj file list
  | lines
  | where {|file| $file | str ends-with ".toml" }
  | each {|file| $root | path join $file }
)

^taplo format ...$files
